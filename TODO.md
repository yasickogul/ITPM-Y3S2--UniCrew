# Viva Readiness Tracker

## Completed
- Discussion list now loads from backend API.
- Post details now use API for fetch, update, delete, comments, and likes.
- Create post now uses API-only flow (no local fallback path).
- Frontend automated tests added (`vitest`).
- Backend automated tests added (`jest`).
- CORS updated to allow `x-user-*` auth headers from frontend.
- UI label consistency fixes applied in discussion create form.

## Remaining Before Presentation
- Create/update project board cards and capture screenshots.
- Make final clean commits with meaningful commit messages.
- Ensure `git status` is clean before presentation.
